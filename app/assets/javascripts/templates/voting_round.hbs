<div class="voting">
  <div class="proposals">
    <h1>Voting for {{text}}</h1>

    {{#with view as RoundView}}
      {{#each visibleProposals}}
        {{render "proposal" this}}
      {{/each}}
    {{/with}}
  </div>
</div>
