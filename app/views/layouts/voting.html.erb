<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Proposals" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "ember-app" %>
    <%= csrf_meta_tags %>
    <% if current_user %>
      <meta name="current-user" content='<%= UserSerializer.new(current_user).to_json(root: false).html_safe %>' />
    <% end %>
  </head>

  <body>
    <div id="app"></div>
  </body>
</html>
